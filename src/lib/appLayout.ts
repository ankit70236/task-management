/** Tighter horizontal padding for header + main scroll area only. */
export const appTightGutter = 'px-3 sm:px-4 md:px-5 lg:px-6 xl:px-8'

/** Header and body rows (not the bottom nav). */
export const appContentFrame = `w-full min-w-0 ${appTightGutter}`

/** Slightly roomier side inset for the fixed footer bar so it feels grounded vs. the tighter page. */
export const appFooterGutter = 'px-4 sm:px-6 md:px-8'

/** FAB: match tight content gutters. */
export const appFloatingRight = 'right-3 sm:right-4 md:right-5 lg:right-6 xl:right-8'
